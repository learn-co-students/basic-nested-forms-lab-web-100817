<%= form_for @recipe do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.fields_for :ingredients do |i| %>
    <%= i.label :quantity %>
    <%= i.text_field :quantity %><br>
    <%= i.label :name %>
    <%= i.text_field :name %>
  <% end %>
<%= f.submit %>
<% end %>
